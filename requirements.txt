# Node.js + TypeScript + Prisma environment

# Core runtime (ensure Node 18+)
node >= 18.0.0
pnpm >= 9.0.0

# --- Project dependencies ---
@prisma/client==6.0.0
prisma==6.0.0
tsx==4.19.0
typescript==5.6.3

# Optional: utilities you might add later
dotenv==16.4.5
